name: Scheduled Task

on:
  schedule:
    - cron: '0 22 * * *'  # UTC 22:00 = JST 07:00
  workflow_dispatch:       # 手動実行用

permissions:
  contents: write
  id-token: write

jobs:
  # ジョブ1: Claude Codeでクロール・レポート生成・コミット
  collect-and-commit:
    runs-on: ubuntu-latest
    outputs:
      has_report: ${{ steps.check.outputs.has_report }}
      report_date: ${{ steps.check.outputs.report_date }}
    steps:
      - uses: actions/checkout@v4

      - name: Get JST time
        id: jst
        run: |
          echo "date=$(TZ=Asia/Tokyo date +'%Y-%m-%d')" >> $GITHUB_OUTPUT
          echo "time=$(TZ=Asia/Tokyo date +'%H:%M')" >> $GITHUB_OUTPUT
          echo "datetime=$(TZ=Asia/Tokyo date +'%Y-%m-%d %H:%M JST')" >> $GITHUB_OUTPUT

      - name: Run Claude Code Action
        uses: anthropics/claude-code-action/base-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: "--allowedTools Read Write Edit Glob Grep WebFetch WebSearch"
          prompt: |
            定時リサーチタスクを実行してください。

            現在時刻: ${{ steps.jst.outputs.datetime }}
            レポート日付: ${{ steps.jst.outputs.date }}

            1. config/sources.json を読み込み、有効なソース（enabled: true）をクロール
            2. data/latest.json で差分検出（新着記事のみ対象）
            3. 新着記事があれば要約を生成
            4. data/reports/${{ steps.jst.outputs.date }}.md にレポートを保存
            5. data/latest.json を更新
            6. レポートの生成時刻には「${{ steps.jst.outputs.time }} JST」を使用

            CLAUDE.md の「タスク1: 定時リサーチ実行」に従って実行してください。
            注意：Discord/LINE通知は別ジョブで実行されるため、ここでは通知を送らないでください。

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Update crawled data [${{ steps.jst.outputs.date }}]"
          git push

      - name: Check for report
        id: check
        run: |
          REPORT_DATE="${{ steps.jst.outputs.date }}"
          REPORT_FILE="data/reports/${REPORT_DATE}.md"
          if [ -f "$REPORT_FILE" ]; then
            echo "has_report=true" >> $GITHUB_OUTPUT
          else
            echo "has_report=false" >> $GITHUB_OUTPUT
          fi
          echo "report_date=${REPORT_DATE}" >> $GITHUB_OUTPUT

  # ジョブ2: 通知メッセージ生成・送信
  notify:
    needs: collect-and-commit
    if: needs.collect-and-commit.outputs.has_report == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Generate notification messages with Claude Code
        uses: anthropics/claude-code-action/base-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: "--allowedTools Read Write Edit Glob Grep WebFetch WebSearch"
          show_full_output: true
          prompt: |
            通知メッセージを生成してファイルに保存してください。

            レポートファイル: data/reports/${{ needs.collect-and-commit.outputs.report_date }}.md
            リポジトリURL: https://github.com/${{ github.repository }}

            処理手順：
            1. レポートファイルを読み込む
            2. 記事タイトルを日本語に翻訳（英語の場合）
            3. Discord向けメッセージを生成して ./discord_message.txt に保存（カレントディレクトリ直下、/tmp/は使用禁止）
            4. LINE向けメッセージを生成して ./line_message.txt に保存（カレントディレクトリ直下、/tmp/は使用禁止）

            Discord向けメッセージフォーマット:
            ```
            📰 AI最新ニュース (YYYY-MM-DD)
            ━━━━━━━━━━━━━━━━━━━━

            🔹 ソース名
            • 記事タイトル（日本語訳）

            ━━━━━━━━━━━━━━━━━━━━
            📄 詳細: [リポジトリURL]/blob/main/data/reports/[日付].md
            ```

            LINE向けメッセージフォーマット（シンプルに）:
            ```
            📰 AI最新ニュース
            YYYY-MM-DD

            ■ ソース名
            ・記事タイトル（日本語訳）

            詳細: [リポジトリURL]/blob/main/data/reports/[日付].md
            ```

            重要：
            - 記事タイトルは必ず日本語で表示すること
            - ファイルはプレーンテキストで保存（JSONエスケープ不要）
            - 環境変数は参照しないこと

      - name: Debug - List files
        run: |
          echo "Current directory: $(pwd)"
          echo "Files in current directory:"
          ls -la
          echo "---"
          echo "Looking for message files:"
          find . -name "*message*.txt" 2>/dev/null || echo "No message files found"

      - name: Send Discord notification
        if: env.DISCORD_WEBHOOK_URL != ''
        run: |
          if [ -f discord_message.txt ]; then
            MESSAGE=$(cat discord_message.txt)
            # JSONエスケープ
            MESSAGE_JSON=$(echo "$MESSAGE" | python3 -c 'import json,sys; print(json.dumps(sys.stdin.read()))')

            curl -X POST \
              -H "Content-Type: application/json" \
              -d "{\"content\":${MESSAGE_JSON}}" \
              "$DISCORD_WEBHOOK_URL"

            echo "Discord notification sent"
          else
            echo "Discord message file not found"
          fi
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

      - name: Send LINE notification
        if: env.LINE_CHANNEL_ACCESS_TOKEN != '' && env.LINE_USER_ID != ''
        run: |
          if [ -f line_message.txt ]; then
            MESSAGE=$(cat line_message.txt)
            # JSONエスケープ
            MESSAGE_JSON=$(echo "$MESSAGE" | python3 -c 'import json,sys; print(json.dumps(sys.stdin.read()))')

            curl -X POST https://api.line.me/v2/bot/message/push \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer $LINE_CHANNEL_ACCESS_TOKEN" \
              -d "{\"to\":\"$LINE_USER_ID\",\"messages\":[{\"type\":\"text\",\"text\":$MESSAGE_JSON}]}"

            echo "LINE notification sent"
          else
            echo "LINE message file not found"
          fi
        env:
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
